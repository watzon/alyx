name: stripe-webhook
runtime: node
timeout: 30s
memory: 256mb
env:
  STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
hooks:
  - type: webhook
    verification:
      type: hmac-sha256
      header: X-Stripe-Signature
      secret: ${STRIPE_WEBHOOK_SECRET}
