version: 1

collections:
  users:
    fields:
      id:
        type: uuid
        primary: true
        default: auto
      email:
        type: string
        unique: true
      name:
        type: string
      created_at:
        type: timestamp
        default: now
